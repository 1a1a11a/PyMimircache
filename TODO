documentation add:
eviction stat
cachecow evicitonPlot
reader unique_elements
reader get_request_num_distribution

switch to use prepPlotParams in utils

KNOWN Problem:
if plain data, if label is length 1, there will be problem in get_last_access_dist (for py_optimal)
